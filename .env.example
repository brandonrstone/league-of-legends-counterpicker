# Optional. The live lobby uses the local League Client API and does not need this.
# Development keys expire every 24 hours. Never commit a real key.
RIOT_API_KEY=
# Platform routing value, e.g. na1, euw1, eun1, kr, br1, la1, la2, oc1, tr1, ru, jp1
RIOT_PLATFORM=na1
